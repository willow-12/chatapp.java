Multi-Client TCP Server in Java

This is a simple Java TCP server that can handle multiple clients at the same time. Each client can send messages to the server, and the server prints the messages and replies automatically. To use it, first start the server (java Server) and then run one or more clients (java Client) in separate terminals.
